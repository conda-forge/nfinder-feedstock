{% set name = "nfinder" %}
{% set version = "0.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/nfinder-{{ version }}.tar.gz
  sha256: aa4634d98423d8888532c2b2f58595914d0d1a578c6ff795b5144f6f25673772

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python >=3.7
    - setuptools_scm
  run:
    - matplotlib-base
    - napari
    - napari-plugin-engine
    - numpy
    - pandas
    - python >=3.7
    - scikit-image
    - scipy

test:
  imports:
    - nfinder
  commands:
    - pip list
    - pip check
  requires:
    - pip

about:
  home: https://github.com/santi-rodriguez/nfinder
  summary: Automatic inference of neighboring cells based on their Delaunay triangulation.
  license: MIT
  license_file: LICENSE.txt

extra:
  recipe-maintainers:
    - conda-forge/napari
    - santi-rodriguez
